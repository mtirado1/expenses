
## Expense tracker

A simple GO CLI for keeping track of expenses.

Income / expenses are read from a text file. See `example.txt` for reference.

Export expenses to csv:

```
./expenses -csv expenses.txt
```
